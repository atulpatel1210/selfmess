<IfModule mod_rewrite.c>
    RewriteEngine On

    # If the requested path/file exists, serve it directly
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d

    # Otherwise, redirect all requests to the public folder
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
